<%= form_for(@threed) do |f| %>
  <% if @threed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@threed.errors.count, "error") %> prohibited this threed from being saved:</h2>

      <ul>
      <% @threed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label "3D" %><br>
    <%= f.text_field :threed, :class=>"wide-text" %>
  </p>
  <p>
    <%= f.label "Notes" %><br>
    <%= f.text_area :notes, :cols=>"30", :rows=>"5", :class=>"wide-text"  %>
  </p>

<div class="float-save"><input type="submit" value="Save" class="button"></div>
<div id="top-save"><input type="submit" value="" class="button"></div>
<% end %>
